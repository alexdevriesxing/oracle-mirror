{
  "name": "oracle-mirror",
  "version": "1.0.0",
  "description": "A fantasy‑themed fortune telling app running on Cloudflare Workers and Workers AI.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --format=esm --platform=neutral",
    "dev": "wrangler dev",
    "publish": "wrangler publish"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240506.0",
    "@types/node": "^25.9.3",
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0"
  }
}
